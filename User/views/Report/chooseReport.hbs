<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Create Report - Choose method</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet"/>
  <style>
    :root{ --bg:#f3f6f9; --card:#fff; --accent:#06b6d4; --accent-2:#0ea5a4; --muted:#64748b; --radius:14px; --ring: rgba(6,182,212,0.25) }
    body{margin:0;font-family:Inter,system-ui,Segoe UI,Roboto,Arial;background:
      radial-gradient(900px 400px at 0% 0%, rgba(6,182,212,0.06), transparent 60%),
      linear-gradient(180deg,var(--bg),#fff)}
    .wrap{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:28px}
    .panel{background:var(--card);padding:28px;border-radius:var(--radius);max-width:820px;width:100%;text-align:center;box-shadow:0 16px 44px rgba(2,6,23,0.08);border:1px solid rgba(2,6,23,0.06)}
    .panel h2{margin:0 0 6px 0;font-weight:800}
    .opts{display:flex;gap:18px;justify-content:center;margin-top:18px;flex-wrap:wrap}
    .opt{flex:1;min-width:280px;padding:18px;border-radius:12px;border:1px solid rgba(7,16,35,0.08);cursor:pointer;text-decoration:none;color:inherit;display:flex;gap:14px;align-items:flex-start;position:relative;transition:transform .18s ease, box-shadow .18s ease, border-color .18s ease, background .18s ease;background:linear-gradient(180deg,#ffffff,#f8fafc)}
    .opt:focus-visible{outline:none;box-shadow:0 0 0 6px var(--ring)}
    .opt:hover{transform:translateY(-2px);box-shadow:0 14px 40px rgba(2,6,23,0.10);border-color:rgba(7,16,35,0.16);background:#ffffff}
    .opt .icon{width:48px;height:48px;border-radius:12px;display:flex;align-items:center;justify-content:center;background:linear-gradient(135deg,#ecfeff,#dbeafe);color:#0ea5a4;box-shadow:0 10px 24px rgba(6,182,212,0.10);flex-shrink:0}
    .opt .icon i{font-size:22px}
    .opt .text{display:flex;flex-direction:column;align-items:flex-start}
    .opt h3{margin:0 0 6px 0;font-weight:900}
    .muted{color:var(--muted);font-size:14px}
    .opt .arrow{margin-left:auto;color:#94a3b8;align-self:center}
    .opt .arrow i{font-size:16px}
    .btn{display:inline-flex;align-items:center;gap:8px;margin-top:18px;padding:10px 16px;background:linear-gradient(90deg,var(--accent),var(--accent-2));color:#000000;border-radius:12px;text-decoration:none;font-weight:900;box-shadow:0 12px 32px rgba(6,182,212,0.18);transition:transform .18s ease, box-shadow .18s ease}
    .btn:hover{transform:translateY(-2px);box-shadow:0 16px 42px rgba(6,182,212,0.22)}
    .btn i{font-size:14px}
    @media (max-width:640px){
      .opt{min-width:100%}
    }
  </style>
</head>
<body>
  {{> header}}

  <div class="wrap">
    <div class="panel">
      <h2>Create a new report</h2>
      <p class="muted">Choose how you'd like to submit the photo of the issue</p>

      <div class="opts">
        <a class="opt" href="/report/upload">
          <div class="icon" aria-hidden="true"><i class="fas fa-cloud-upload-alt"></i></div>
          <div class="text">
            <h3>Upload from browser</h3>
            <div class="muted">Choose an image file saved on your device</div>
          </div>
          <div class="arrow" aria-hidden="true"><i class="fas fa-chevron-right"></i></div>
        </a>

        <a class="opt" href="/report/camera">
          <div class="icon" aria-hidden="true"><i class="fas fa-camera"></i></div>
          <div class="text">
            <h3>Use camera</h3>
            <div class="muted">Open your camera, capture a photo and automatically attach your address</div>
          </div>
          <div class="arrow" aria-hidden="true"><i class="fas fa-chevron-right"></i></div>
        </a>
      </div>

      <a class="btn" href="/dashboard"><i class="fas fa-arrow-left"></i> Back to dashboard</a>
    </div>
  </div>

  {{> footer}}
</body>
</html>