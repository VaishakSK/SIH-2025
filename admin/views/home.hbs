<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>CivicSense Admin - City Operations Command</title>
  <style>
    :root{ --bg-1:#0f172a; --accent:#06b6d4; --accent-2:#0ea5a4; --muted:#94a3b8; --radius:14px; --shadow:0 10px 30px rgba(2,6,23,0.12); --glass: rgba(255,255,255,0.65); --border: rgba(15,23,42,0.08)}
    *{box-sizing:border-box}
    html,body{height:100%;margin:0;font-family:Inter,system-ui,Helvetica,Arial;background:
      radial-gradient(1200px 400px at 10% 10%, rgba(6,182,212,0.05), transparent),
      radial-gradient(1000px 380px at 90% 0%, rgba(14,165,164,0.05), transparent),
      linear-gradient(180deg,#f5f7fb,#eef2f6);color:var(--bg-1)}
    .wrap{min-height:100vh;display:flex;flex-direction:column;position:relative;overflow:hidden}
    /* animated gradient orbs */
    .ornaments{position:absolute;inset:0;pointer-events:none;}
    .orb{position:absolute;filter:blur(60px);opacity:0.35;animation:float 18s ease-in-out infinite}
    .orb.one{width:420px;height:420px;background:radial-gradient(circle at 30% 30%, #67e8f9, transparent 60%);top:-120px;left:-120px}
    .orb.two{width:520px;height:520px;background:radial-gradient(circle at 70% 40%, #a7f3d0, transparent 60%);bottom:-160px;right:-160px;animation-delay:4s}
    @keyframes float{0%,100%{transform:translateY(0) translateX(0)}50%{transform:translateY(18px) translateX(-10px)}}

    .nav{display:flex;justify-content:space-between;align-items:center;padding:16px 20px;background:linear-gradient(180deg,rgba(255,255,255,0.9),#fff);backdrop-filter:saturate(140%) blur(6px);box-shadow:var(--shadow);border-bottom:1px solid var(--border)}
    .brand{font-weight:900}
    .links{display:flex;gap:12px}
    .links a{padding:10px 12px;border-radius:10px;text-decoration:none;color:var(--bg-1);font-weight:700;transition:transform .15s ease, box-shadow .2s ease}
    .links a.primary{background:linear-gradient(90deg,var(--accent),var(--accent-2));color:#fff;box-shadow:0 10px 30px rgba(6,182,212,0.12)}
    .links a:hover{transform:translateY(-1px);box-shadow:0 8px 22px rgba(2,6,23,0.08)}

    .hero{max-width:1200px;margin:32px auto 0 auto;padding:24px;background:
      linear-gradient(180deg, rgba(255,255,255,0.92), rgba(255,255,255,0.86));
      border-radius:22px;box-shadow:0 30px 60px rgba(2,6,23,0.10);border:1px solid var(--border);backdrop-filter: blur(4px)}
    .hero h1{margin:0 0 8px 0;font-size:34px}
    .hero .sub{color:var(--muted)}
    .cta{display:flex;gap:12px;margin-top:16px}
    .btn{padding:10px 14px;border-radius:10px;border:0;cursor:pointer;font-weight:800}
    .btn-primary{background:linear-gradient(90deg,var(--accent),var(--accent-2));color:white;box-shadow:0 10px 30px rgba(6,182,212,0.18)}
    .btn-ghost{background:transparent;border:1px solid rgba(15,23,42,0.06);color:var(--bg-1)}

    .kpis{max-width:1200px;margin:16px auto;display:grid;grid-template-columns:repeat(4,1fr);gap:14px}
    .kpi{padding:18px;border-radius:16px;background:linear-gradient(180deg,rgba(255,255,255,0.9),#fff);box-shadow:0 20px 50px rgba(2,6,23,0.07);border:1px solid var(--border);position:relative;overflow:hidden}
    .kpi::after{content:"";position:absolute;inset:auto -20% -40% -20%;height:60%;background:radial-gradient(120px 40px at 50% 0%, rgba(6,182,212,0.18), transparent);filter:blur(20px)}
    .kpi .label{font-size:12px;color:var(--muted);letter-spacing:.2px;text-transform:uppercase}
    .kpi .value{font-size:28px;font-weight:900;margin-top:6px}
    .kpi:hover{transform:translateY(-2px);transition:transform .2s ease, box-shadow .2s ease;box-shadow:0 26px 60px rgba(2,6,23,0.12)}

    .sections{max-width:1200px;margin:0 auto 40px auto;display:grid;grid-template-columns:2fr 1fr;gap:16px}
    .panel{padding:16px;border-radius:16px;background:linear-gradient(180deg,rgba(255,255,255,0.9),#fff);box-shadow:0 20px 50px rgba(2,6,23,0.07);border:1px solid var(--border)}
    .panel h3{margin:0 0 8px 0}
    .features{display:grid;grid-template-columns:1fr;gap:10px}
    .feature{padding:12px;border-radius:12px;border:1px solid var(--border);background:linear-gradient(180deg,rgba(255,255,255,0.95),transparent);transition:transform .2s ease, box-shadow .2s ease}
    .feature:hover{transform:translateY(-2px);box-shadow:0 18px 40px rgba(2,6,23,0.08)}
    .muted{color:var(--muted)}

    /* decorative grid overlay */
    .grid-overlay{position:absolute;inset:0;background-image: linear-gradient(rgba(2,6,23,0.04) 1px, transparent 1px), linear-gradient(90deg, rgba(2,6,23,0.04) 1px, transparent 1px);background-size: 26px 26px;mask-image: radial-gradient(80% 60% at 50% 0%, black, transparent 70%);pointer-events:none}

    @media (prefers-reduced-motion:reduce){
      .orb{animation:none}
      .links a, .kpi, .feature{transition:none}
    }

    @media (max-width:900px){.grid{grid-template-columns:1fr}}
  </style>
  </head>
<body>
  <div class="wrap">
    <div class="ornaments" aria-hidden="true">
      <div class="orb one"></div>
      <div class="orb two"></div>
      <div class="grid-overlay"></div>
    </div>
    <nav class="nav">
      <div class="brand">CivicConnect Admin</div>
      <div class="links">
        <a href="/admin/login" class="primary">Sign in</a>
      </div>
    </nav>
    <main class="hero">
      <h1>City Operations Command for CivicSense</h1>
      <div class="sub">One pane of glass for incidents, service levels, and citizen satisfaction.</div>
      
    </main>

    <section class="kpis" aria-label="Key performance indicators">
      <div class="kpi"><div class="label">Active Incidents</div><div class="value">128</div></div>
      <div class="kpi"><div class="label">Avg. Response Time</div><div class="value">3h 12m</div></div>
      <div class="kpi"><div class="label">SLA Compliance</div><div class="value">92%</div></div>
      <div class="kpi"><div class="label">Citizen Satisfaction</div><div class="value">4.3/5</div></div>
    </section>

    <section class="sections">
      <div class="panel">
        <h3>Live Incidents Map</h3>
        <div class="muted">Embed your GIS/Map provider here to visualize complaints and city assets.</div>
        <div style="margin-top:10px;height:280px;border-radius:12px;background:repeating-linear-gradient(45deg,#f0f9ff,#f0f9ff 10px,#e2e8f0 10px,#e2e8f0 20px);"></div>
      </div>
      <div class="panel">
        <h3>Must-have modules</h3>
        <div class="features">
          <div class="feature"><strong>Incident Intake & Triage</strong><div class="muted">Multi-channel capture, deduplication, prioritization.</div></div>
          <div class="feature"><strong>Work Orders & Routing</strong><div class="muted">Assign to departments, track field crew progress.</div></div>
          <div class="feature"><strong>SLA & Escalations</strong><div class="muted">Auto-escalate breaching tickets, SLA dashboards.</div></div>
          <div class="feature"><strong>Citizen Communication</strong><div class="muted">Status updates, OTP verification, feedback surveys.</div></div>
          <div class="feature"><strong>Analytics & Heatmaps</strong><div class="muted">Identify hotspots, seasonal trends, repeat offenders.</div></div>
          <div class="feature"><strong>Integrations</strong><div class="muted">Email/SMS gateways, GIS, 311/helpline, IVR, WhatsApp.</div></div>
          <div class="feature"><strong>Audit & Governance</strong><div class="muted">Role-based access, logs, data retention policies.</div></div>
        </div>
      </div>
    </section>
  </div>
</body>
</html>
